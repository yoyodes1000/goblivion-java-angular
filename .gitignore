# ─────────────────────────────────────────────────────────────
# Contenu sous droits — Goblivion © Goblivion Games
# Les scans de cartes restent en local et ne doivent JAMAIS partir
# sur le dépôt public. Le code les référence par nom de fichier,
# il ne les embarque pas.
# ─────────────────────────────────────────────────────────────
cartes bleues/
cartes boss/
cartes dorees/
cartes ennemis-objets/
cartes roi-reines/
medias/
*.pdf

# Scans des plateaux : meme statut que les scans de cartes. `plateaux/` est la
# copie de travail, `frontend/public/plateaux/` celle que le serveur Angular
# sert au navigateur. Les deux restent locales.
plateaux/
frontend/public/plateaux/

# Les scans de cartes devront eux aussi passer par le dossier servi par Angular.
# Les regles ci-dessus les attrapent deja tant que les dossiers gardent leur nom
# exact ; cette regle-ci tient meme si on les reorganise en `cartes/dorees/`.
frontend/public/cartes*/

# Donnees de cartes normalisees : noms, actions et valeurs sont du contenu
# Goblivion Games au meme titre que les scans. Seul le schema est versionne,
# dans docs/modele-cartes.md, et scripts/valider-cartes.mjs verifie que les
# donnees locales le respectent.
data/cartes/

# ─────────────────────────────────────────────────────────────
# Backend Java / Maven
# ─────────────────────────────────────────────────────────────
target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
*.war
*.log
application-local.properties
application-local.yml

# ─────────────────────────────────────────────────────────────
# Frontend Angular / Node
# ─────────────────────────────────────────────────────────────
node_modules/
dist/
.angular/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# ─────────────────────────────────────────────────────────────
# Éditeurs & OS
# ─────────────────────────────────────────────────────────────
.idea/
*.iml
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# ─────────────────────────────────────────────────────────────
# Outillage local
# ─────────────────────────────────────────────────────────────
.claude/settings.local.json
organiseur-sauvegarde-*.json
